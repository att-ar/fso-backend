{
    "name": "part3_notes",
    "version": "0.0.1",
    "description": "part 3 notes",
    "main": "index.js",
    "scripts": {
        "start": "node index.js",
        "dev": "nodemon index.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "build:ui": "cd frontend && rm -rf build && cd ../../../fso/fso-learning/part2 && npm run build && cp -r build ../../../fso-backend/part3_notes/frontend",
        "deploy": "git add . && git commit -m uibuild && git push",
        "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
    },
    "repository": {
        "type": "git",
        "url": "fso-backend"
    },
    "author": "Attar Aziz",
    "license": "MIT",
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "nodemon": "^2.0.21"
    }
}
